# =====================================================
# My Trip - 환경변수 템플릿
# =====================================================
# 이 파일을 복사하여 .env 파일을 생성하고 실제 값을 입력하세요.
# cp .env.example .env

# =====================================================
# 한국관광공사 API (KorService2)
# =====================================================
# 발급: https://www.data.go.kr/data/15101578/openapi.do
# 클라이언트 사이드에서 사용 (브라우저에서 직접 API 호출)
NEXT_PUBLIC_TOUR_API_KEY=your_tour_api_key_here

# 서버 사이드에서 사용 (Next.js API Routes, Server Components)
# PRD 8.4: "한국 관광공사 에러가 난다면 NEXT_PUBLIC_TOUR_API_KEY가 인식 안 될 때" 대비
TOUR_API_KEY=your_tour_api_key_here

# =====================================================
# 네이버 지도 API v3 (Naver Cloud Platform)
# =====================================================
# 발급: https://www.ncloud.com/product/applicationService/maps
# 주의: ncpKeyId 사용 (구 ncpClientId 아님)
# 월 10,000,000건 무료 (신용카드 등록 필수)
NEXT_PUBLIC_NAVER_MAP_CLIENT_ID=your_naver_map_client_id_here

# =====================================================
# Clerk Authentication
# =====================================================
# 발급: https://dashboard.clerk.com/
# 클라이언트 사이드 Clerk 키
NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY=pk_test_xxxxx

# 서버 사이드 Clerk 비밀 키 (절대 클라이언트에 노출 금지)
CLERK_SECRET_KEY=sk_test_xxxxx

# =====================================================
# Supabase
# =====================================================
# 발급: https://supabase.com/dashboard/project/_/settings/api
# Supabase 프로젝트 URL
NEXT_PUBLIC_SUPABASE_URL=https://your-project-id.supabase.co

# Supabase 익명 키 (클라이언트에서 사용)
NEXT_PUBLIC_SUPABASE_ANON_KEY=your_supabase_anon_key_here

# Supabase 서비스 역할 키 (서버 전용, 절대 클라이언트에 노출 금지)
SUPABASE_SERVICE_ROLE_KEY=your_supabase_service_role_key_here

# 스토리지 버킷 이름 (기본값: uploads)
NEXT_PUBLIC_STORAGE_BUCKET=uploads

# =====================================================
# 보안 주의사항
# =====================================================
# 1. NEXT_PUBLIC_ 접두사가 붙은 변수만 클라이언트에 노출됩니다.
# 2. 서버 전용 키(TOUR_API_KEY, CLERK_SECRET_KEY, SUPABASE_SERVICE_ROLE_KEY)는
#    절대 클라이언트에 노출되지 않도록 주의하세요.
# 3. .env 파일은 .gitignore에 포함되어 있어야 합니다.
# 4. 프로덕션 환경에서는 Vercel 대시보드에서 환경변수를 설정하세요.
